# Requires devenv and direnv. Devenv owns the activation logic and watches.
if ! has devenv; then
  log_error "devenv is required: https://devenv.sh/getting-started/"
  return 1
fi

eval "$(devenv direnvrc)"
use devenv

dotenv_if_exists .env.local
