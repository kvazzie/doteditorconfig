# devenv
.devenv/
.devenv.flake.nix
devenv.local.nix
.devenv.lock
result
result-*

# direnv
.direnv/
.envrc.local

# nix (keep flake.lock committed for reproducibility — uncomment to ignore in apps)
# flake.lock

# helix
.helix/runtime/

# os
.DS_Store
Thumbs.db
